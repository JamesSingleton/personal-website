import Page from '@/components/Page';
import { NAME, WEBSITE_DESCRIPTION } from '@/lib/constants';

const Projects = () => (
  <Page title={`Projects | ${NAME} - ${WEBSITE_DESCRIPTION}`}>
    <h1>Projects</h1>
  </Page>
);

export default Projects;